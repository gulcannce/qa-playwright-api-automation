# API tests

These are example Playwright API tests.

Run only API tests:

```bash
npx playwright test api-tests
```

Files:
- `api.spec.js` — example health checks against `https://www.saucedemo.com` and demo login tests against `https://reqres.in`.
- `test-data/users.json` — demo credentials for ReqRes.
